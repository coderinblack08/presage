mutation CreateSoundbite(
  $thumbnail: Upload
  $audio: Upload!
  $data: SoundbiteArgs!
) {
  createSoundbite(audio: $audio, data: $data, thumbnail: $thumbnail) {
    id
    title
    description
    thumbnail
    audio
    length
    createdAt
    updatedAt
  }
}
